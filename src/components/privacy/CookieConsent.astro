<div class="cookie-consent" data-cookie-consent>
  <section
    class="cookie-cloud cookie-cloud--banner"
    data-cookie-banner
    role="region"
    aria-labelledby="cookie-banner-title"
    aria-describedby="cookie-banner-description"
    hidden
  >
    <div class="cookie-cloud__icon" aria-hidden="true">
      <svg viewBox="0 0 48 48">
        <path d="M36.8 25.2a8.5 8.5 0 0 1-9.7-10.7A15.5 15.5 0 1 0 36.8 25.2Z" />
        <circle cx="18" cy="20" r="2.2" />
        <circle cx="23.5" cy="30" r="2.2" />
        <circle cx="14.5" cy="31.5" r="1.8" />
      </svg>
    </div>

    <div class="cookie-cloud__copy">
      <h2 id="cookie-banner-title">Usamos cookies</h2>
      <p id="cookie-banner-description">
        Usamos PostHog para analítica, mapas de calor y grabaciones de sesión con los formularios
        ocultos. Solo se activa si aceptas y puedes retirar el consentimiento cuando quieras.
      </p>
      <a href="/cookies">Más información en la política de cookies</a>
    </div>

    <div class="cookie-cloud__actions" aria-label="Opciones de cookies">
      <button class="cookie-button cookie-button--primary" type="button" data-cookie-accept>
        Aceptar todas
      </button>
      <button class="cookie-button cookie-button--secondary" type="button" data-cookie-reject>
        Solo necesarias
      </button>
      <button class="cookie-button cookie-button--configure" type="button" data-cookie-configure>
        Configurar
      </button>
    </div>
  </section>

  <div class="cookie-preferences" data-cookie-preferences hidden>
    <div class="cookie-preferences__backdrop" aria-hidden="true"></div>
    <section
      class="cookie-preferences__panel"
      role="dialog"
      aria-modal="true"
      aria-labelledby="cookie-preferences-title"
      aria-describedby="cookie-preferences-description"
      tabindex="-1"
      data-cookie-preferences-panel
    >
      <header class="cookie-preferences__header">
        <div class="cookie-cloud__icon" aria-hidden="true">
          <svg viewBox="0 0 48 48">
            <path d="M36.8 25.2a8.5 8.5 0 0 1-9.7-10.7A15.5 15.5 0 1 0 36.8 25.2Z" />
            <circle cx="18" cy="20" r="2.2" />
            <circle cx="23.5" cy="30" r="2.2" />
            <circle cx="14.5" cy="31.5" r="1.8" />
          </svg>
        </div>
        <div>
          <h2 id="cookie-preferences-title">Preferencias de privacidad</h2>
          <p id="cookie-preferences-description">Elige qué tecnologías puede utilizar Altaria Lights.</p>
        </div>
        <button class="cookie-preferences__close" type="button" data-cookie-close aria-label="Cerrar preferencias">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="m6 6 12 12M18 6 6 18" /></svg>
        </button>
      </header>

      <div class="cookie-preferences__categories">
        <article class="cookie-category">
          <div>
            <h3>Necesarias</h3>
            <p>Necesarias para el funcionamiento y para recordar tus preferencias de privacidad.</p>
          </div>
          <span class="cookie-category__always" aria-label="Cookies necesarias siempre activas">Siempre activas</span>
        </article>

        <article class="cookie-category">
          <div>
            <h3>Analítica — PostHog</h3>
            <p>Incluye eventos, autocapture, mapas de calor y Session Replay. Los inputs se enmascaran y el formulario del diagnóstico se bloquea por completo en las grabaciones.</p>
          </div>
          <label class="cookie-switch">
            <span class="sr-only">Permitir analítica de PostHog</span>
            <input type="checkbox" data-cookie-analytics />
            <span class="cookie-switch__track" aria-hidden="true"><span></span></span>
          </label>
        </article>
      </div>

      <p class="cookie-preferences__note">
        PostHog no se carga ni captura información mientras la analítica esté desactivada.
        <a href="/cookies">Consulta todos los detalles</a>.
      </p>

      <div class="cookie-preferences__actions">
        <button class="cookie-button cookie-button--primary" type="button" data-cookie-panel-accept>
          Aceptar todas
        </button>
        <button class="cookie-button cookie-button--primary" type="button" data-cookie-panel-reject>
          Solo necesarias
        </button>
        <button class="cookie-button cookie-button--save" type="button" data-cookie-save>
          Guardar preferencias
        </button>
      </div>
    </section>
  </div>

  <p class="sr-only" role="status" aria-live="polite" data-cookie-status></p>
</div>

<script>
  import { initCookieConsent } from '../../scripts/cookie-consent';
  initCookieConsent();
</script>

<style>
  .cookie-consent {
    --cookie-navy: #0b3265;
    --cookie-blue: #1267bf;
    --cookie-sky: #eaf5ff;
    --cookie-white: rgba(250, 253, 255, 0.97);
    position: relative;
    z-index: 2147483000;
    color: var(--cookie-navy);
  }

  .cookie-consent [hidden] {
    display: none !important;
  }

  .cookie-cloud--banner {
    position: fixed;
    right: max(18px, env(safe-area-inset-right));
    bottom: max(18px, env(safe-area-inset-bottom));
    left: max(18px, env(safe-area-inset-left));
    display: grid;
    width: min(790px, calc(100vw - 36px));
    min-height: 124px;
    margin-inline: auto;
    padding: 18px 20px;
    grid-template-columns: 52px minmax(265px, 1fr) minmax(292px, 0.82fr);
    align-items: center;
    gap: 16px;
    border: 1px solid rgba(255, 255, 255, 0.82);
    border-radius: 32px;
    background:
      radial-gradient(circle at 76% 6%, rgba(255, 239, 215, 0.44), transparent 31%),
      linear-gradient(145deg, rgba(253, 254, 255, 0.96), rgba(241, 248, 255, 0.93));
    box-shadow:
      0 20px 44px -27px rgba(5, 40, 80, 0.52),
      inset 0 1px 0 rgba(255, 255, 255, 0.96);
    -webkit-backdrop-filter: blur(16px) saturate(1.08);
    backdrop-filter: blur(16px) saturate(1.08);
    isolation: isolate;
  }

  .cookie-cloud--banner::before,
  .cookie-cloud--banner::after {
    content: '';
    position: absolute;
    z-index: -1;
    border: 1px solid rgba(255, 255, 255, 0.88);
    background: rgba(248, 252, 255, 0.98);
    box-shadow: 0 16px 38px -31px rgba(5, 40, 80, 0.7);
  }

  .cookie-cloud--banner::before {
    top: -17px;
    left: 29px;
    width: 92px;
    height: 58px;
    border-radius: 54% 58% 48% 52%;
  }

  .cookie-cloud--banner::after {
    right: 31px;
    bottom: -11px;
    width: 86px;
    height: 49px;
    border-radius: 52%;
  }

  .cookie-cloud--banner .cookie-cloud__icon {
    width: 52px;
    height: 52px;
    box-shadow: inset 0 1px 0 #fff, 0 10px 20px -15px rgba(16, 77, 137, 0.68);
  }

  .cookie-cloud--banner .cookie-cloud__icon svg {
    width: 32px;
    height: 32px;
  }

  .cookie-cloud__icon {
    display: grid;
    width: 64px;
    height: 64px;
    place-items: center;
    border: 1px solid rgba(255, 255, 255, 0.9);
    border-radius: 50%;
    color: #1573d0;
    background: linear-gradient(145deg, #e5f3ff, #d5eaff);
    box-shadow: inset 0 1px 0 #fff, 0 12px 22px -18px rgba(16, 77, 137, 0.7);
  }

  .cookie-cloud__icon svg {
    width: 38px;
    height: 38px;
    overflow: visible;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 2.4;
  }

  .cookie-cloud__copy h2,
  .cookie-preferences h2,
  .cookie-preferences h3 {
    margin: 0;
    color: var(--cookie-navy);
  }

  .cookie-cloud__copy h2 {
    font-size: clamp(18px, 1.35vw, 21px);
    line-height: 1.1;
  }

  .cookie-cloud__copy p {
    max-width: 49ch;
    margin: 5px 0 0;
    font-size: 12.25px;
    line-height: 1.42;
    color: #315a84;
  }

  .cookie-cloud__copy a,
  .cookie-preferences__note a {
    display: inline-block;
    margin-top: 4px;
    font-size: 10.75px;
    font-weight: 650;
    color: #125ca7;
    text-underline-offset: 3px;
  }

  .cookie-cloud__actions,
  .cookie-preferences__actions {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 9px;
  }

  .cookie-cloud--banner .cookie-cloud__actions {
    gap: 7px;
  }

  .cookie-cloud--banner .cookie-button {
    min-height: 40px;
    padding-inline: 13px;
    font-size: 11.5px;
  }

  .cookie-button {
    min-height: 46px;
    padding: 0 16px;
    border: 1px solid transparent;
    border-radius: 999px;
    font: inherit;
    font-size: 12.5px;
    font-weight: 720;
    line-height: 1.1;
    cursor: pointer;
    transition: transform 150ms ease, box-shadow 150ms ease, background-color 150ms ease;
  }

  .cookie-button--primary {
    color: #fff;
    background: linear-gradient(135deg, #1762ad, #093b76);
    box-shadow: 0 10px 19px -14px rgba(5, 44, 88, 0.82);
  }

  .cookie-cloud--banner .cookie-button--secondary {
    color: #0c4d89;
    border-color: rgba(20, 99, 174, 0.32);
    background: rgba(251, 253, 255, 0.86);
    box-shadow: 0 9px 17px -15px rgba(5, 44, 88, 0.72);
  }

  .cookie-button--configure,
  .cookie-button--save {
    grid-column: 1 / -1;
    color: #0d4b87;
    border-color: rgba(25, 102, 175, 0.25);
    background: rgba(255, 255, 255, 0.74);
  }

  .cookie-cloud--banner .cookie-button--configure {
    color: #315f8e;
    border-color: rgba(25, 102, 175, 0.17);
    background: rgba(255, 255, 255, 0.54);
  }

  .cookie-button:disabled {
    cursor: wait;
    opacity: 0.62;
  }

  .cookie-button:focus-visible,
  .cookie-preferences__close:focus-visible,
  .cookie-switch input:focus-visible + .cookie-switch__track,
  .cookie-cloud a:focus-visible,
  .cookie-preferences a:focus-visible {
    outline: 3px solid #ffcc62;
    outline-offset: 3px;
  }

  .cookie-preferences {
    position: fixed;
    inset: 0;
    display: grid;
    padding: 24px;
    place-items: center;
  }

  .cookie-preferences__backdrop {
    position: absolute;
    inset: 0;
    background: rgba(5, 31, 61, 0.3);
    backdrop-filter: blur(5px);
  }

  .cookie-preferences__panel {
    position: relative;
    width: min(650px, 100%);
    max-height: calc(100svh - 48px);
    padding: 30px;
    overflow: auto;
    border: 1px solid rgba(255, 255, 255, 0.9);
    border-radius: 36px;
    background:
      radial-gradient(circle at 85% 5%, rgba(255, 237, 207, 0.75), transparent 30%),
      rgba(249, 252, 255, 0.98);
    box-shadow: 0 30px 80px -36px rgba(3, 33, 68, 0.72), inset 0 1px 0 #fff;
  }

  .cookie-preferences__header {
    display: grid;
    grid-template-columns: 58px minmax(0, 1fr) 40px;
    align-items: center;
    gap: 16px;
  }

  .cookie-preferences__header .cookie-cloud__icon {
    width: 58px;
    height: 58px;
  }

  .cookie-preferences__header h2 {
    font-size: clamp(21px, 3vw, 27px);
    line-height: 1.15;
  }

  .cookie-preferences__header p {
    margin: 5px 0 0;
    font-size: 13.5px;
    line-height: 1.4;
    color: #43678d;
  }

  .cookie-preferences__close {
    display: grid;
    width: 40px;
    height: 40px;
    padding: 0;
    place-items: center;
    border: 1px solid rgba(24, 85, 146, 0.13);
    border-radius: 50%;
    color: #174f87;
    background: rgba(255, 255, 255, 0.76);
    cursor: pointer;
  }

  .cookie-preferences__close svg {
    width: 20px;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-width: 1.8;
  }

  .cookie-preferences__categories {
    display: grid;
    margin-top: 24px;
    gap: 11px;
  }

  .cookie-category {
    display: grid;
    min-height: 112px;
    padding: 20px;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: center;
    gap: 18px;
    border: 1px solid rgba(47, 104, 163, 0.13);
    border-radius: 22px;
    background: rgba(238, 247, 255, 0.72);
  }

  .cookie-category h3 {
    font-size: 16px;
  }

  .cookie-category p {
    max-width: 48ch;
    margin: 6px 0 0;
    font-size: 13px;
    line-height: 1.45;
    color: #42678d;
  }

  .cookie-category__always {
    padding: 8px 11px;
    border-radius: 999px;
    font-size: 11px;
    font-weight: 720;
    white-space: nowrap;
    color: #1762a5;
    background: #fff;
  }

  .cookie-switch {
    position: relative;
    display: inline-flex;
    cursor: pointer;
  }

  .cookie-switch input {
    position: absolute;
    width: 1px;
    height: 1px;
    opacity: 0;
  }

  .cookie-switch__track {
    display: block;
    width: 58px;
    height: 32px;
    padding: 3px;
    border: 1px solid rgba(35, 82, 126, 0.18);
    border-radius: 999px;
    background: #c8d6e3;
    transition: background-color 160ms ease;
  }

  .cookie-switch__track span {
    display: block;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    background: #fff;
    box-shadow: 0 3px 9px rgba(17, 55, 94, 0.23);
    transition: transform 160ms ease;
  }

  .cookie-switch input:checked + .cookie-switch__track {
    background: #176bc0;
  }

  .cookie-switch input:checked + .cookie-switch__track span {
    transform: translateX(26px);
  }

  .cookie-preferences__note {
    margin: 16px 2px 0;
    font-size: 12.5px;
    line-height: 1.45;
    color: #4b6d90;
  }

  .cookie-preferences__note a {
    display: inline;
  }

  .cookie-preferences__actions {
    margin-top: 21px;
  }

  .sr-only {
    position: absolute !important;
    width: 1px !important;
    height: 1px !important;
    padding: 0 !important;
    overflow: hidden !important;
    clip: rect(0, 0, 0, 0) !important;
    white-space: nowrap !important;
    border: 0 !important;
  }

  @media (hover: hover) {
    .cookie-button:not(:disabled):hover {
      transform: translateY(-1px);
      box-shadow: 0 13px 23px -15px rgba(5, 44, 88, 0.9);
    }

    .cookie-button--configure:hover,
    .cookie-button--save:hover {
      background: #fff;
    }
  }

  @media (max-width: 820px) {
    .cookie-cloud--banner {
      width: min(720px, calc(100vw - 32px));
      min-height: 0;
      padding: 17px 18px;
      grid-template-columns: 48px minmax(0, 1fr) minmax(262px, 0.9fr);
      gap: 14px;
    }

    .cookie-cloud--banner .cookie-cloud__icon {
      width: 48px;
      height: 48px;
    }

    .cookie-cloud--banner .cookie-cloud__icon svg {
      width: 29px;
      height: 29px;
    }

    .cookie-cloud--banner .cookie-button {
      min-height: 39px;
      padding-inline: 10px;
      font-size: 11px;
    }
  }

  @media (max-width: 680px) {
    .cookie-cloud--banner {
      right: max(12px, env(safe-area-inset-right));
      bottom: max(12px, env(safe-area-inset-bottom));
      left: max(12px, env(safe-area-inset-left));
      width: min(394px, calc(100vw - 24px));
      padding: 20px 18px 17px;
      grid-template-columns: 44px minmax(0, 1fr);
      align-items: start;
      gap: 12px;
      border-radius: 28px;
    }

    .cookie-cloud--banner::before {
      top: -15px;
      left: 25px;
      width: 78px;
      height: 50px;
    }

    .cookie-cloud--banner::after {
      right: 24px;
      bottom: -9px;
      width: 72px;
      height: 42px;
    }

    .cookie-cloud--banner .cookie-cloud__icon {
      width: 44px;
      height: 44px;
    }

    .cookie-cloud--banner .cookie-cloud__icon svg {
      width: 27px;
      height: 27px;
    }

    .cookie-cloud__copy h2 {
      font-size: 18px;
    }

    .cookie-cloud__copy p {
      max-width: 36ch;
      font-size: 12px;
    }

    .cookie-cloud--banner .cookie-cloud__actions {
      grid-column: 1 / -1;
      grid-template-columns: 1fr;
      gap: 7px;
      margin-top: 1px;
    }

    .cookie-cloud--banner .cookie-button,
    .cookie-cloud--banner .cookie-button--configure {
      grid-column: 1;
      min-height: 44px;
      font-size: 12.25px;
    }
  }

  @media (max-width: 560px) {
    .cookie-preferences {
      padding: 14px;
      align-items: end;
    }

    .cookie-preferences__panel {
      width: 100%;
      max-height: calc(100svh - 28px);
      padding: 23px 18px max(20px, env(safe-area-inset-bottom));
      border-radius: 30px;
    }

    .cookie-preferences__header {
      grid-template-columns: 48px minmax(0, 1fr) 38px;
      gap: 11px;
    }

    .cookie-preferences__header .cookie-cloud__icon {
      width: 48px;
      height: 48px;
    }

    .cookie-preferences__header .cookie-cloud__icon svg {
      width: 30px;
      height: 30px;
    }

    .cookie-preferences__header h2 {
      font-size: 19px;
    }

    .cookie-preferences__header p {
      font-size: 12px;
    }

    .cookie-preferences__categories {
      margin-top: 18px;
    }

    .cookie-category {
      min-height: 104px;
      padding: 17px;
      gap: 12px;
    }

    .cookie-category__always {
      max-width: 76px;
      white-space: normal;
      text-align: center;
    }

    .cookie-preferences__actions {
      grid-template-columns: 1fr 1fr;
    }

    .cookie-preferences__actions .cookie-button--save {
      grid-column: 1 / -1;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .cookie-consent *,
    .cookie-consent *::before,
    .cookie-consent *::after {
      scroll-behavior: auto !important;
      transition-duration: 0.001ms !important;
    }
  }
</style>
